// custom Array<10,T,false>
// std::array or std::vector as second template
// operator[], begin(), end()
// max()/min()

#include <iostream>
#include <cstddef>

template<std::size_t s, typename t, bool use_heap>
class Array{
public:
	Array(std::size_t s, t type, bool){
		if(stdarray){
			std::array<s, 
		}
	};


private:
	std::array<s, T> _array;
	std::vector<T> _vector;
};


int main(){

	return 0;
}