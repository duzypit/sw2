#include <iostream>
#include <vector>
#include <algorithm>

// int minmax(int a, int b, int c);
// float minmax(float a, float b, folat c);
// double minmax(double a, dobule b, double c);

// struct Complex {/*impl*/};
   
// Complex minmax(Complex a, Complex b, Complex c);

//rozwiązanie jest bardzo proste
template<typename T>
T minmax(T a, T b, T c){
        if (a >c && a > b) return a;
        /*...*/
}

int main(void){

    // int minimax(int a, int b, int c){
    //     if (a >c && a > b) return a;
    //     /*...*/
    // }



    return 0;
}