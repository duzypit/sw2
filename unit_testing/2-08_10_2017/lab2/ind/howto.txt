#HowTo - podłaczenie google mock bez użycia qtcreatora.

#Instrukcja dla Ubuntu, na bank działa w 13.04+

#jeśli nie masz cmake
sudo apt-get install cmake

#google mock ma w sobie google test
sudo apt-get install google-mock
cd /usr/src/gmock
sudo cmake CMakeLists.txt
sudo make
sudo cp *.a /usr/lib 

#przygotowanie źródeł:
#należy dołączyć nagłówek gmock.h
#znajduje się w katalogu gtest/googlemock/include/gmock/ od prowadzącego

#funkcja main musi wyglądać w ten sposób:

#int main(int argc, char **argv) 
#{
#  ::testing::InitGoogleTest(&argc, argv);
#  return RUN_ALL_TESTS();
#}

#podczas kompilacj należy dodać statyczne biblioteki -pthread -lgtest
#g++ -Wall -Wextra Picture.cpp Rectangle.cpp Triangle.cpp test.cpp -std=c++11 -pthread -lgtest

#żródła: https://github.com/duzypit/sw2/tree/master/unit_testing/2-08_10_2017/lab2/ind
